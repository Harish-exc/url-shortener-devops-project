# URL Shortener Service (Layer 1)

This is a beginner-friendly but MAANG-style structured application used for CI/CD, Docker, Monitoring, and Kubernetes learning.

## Run locally

```bash
pip install -r requirements.txt
uvicorn app.main:app --reload
